# Redesign Test Plan — Live Checklist

**Date:** 2026-05-05
**Status:** In progress
**Test target branches:**
- Backend: `slice-06-focus-lock`
- Mac: `slice-13-cleanup`
- iOS: `slice-12-puck-alarm-only`

Update this doc as you test. Mark `[x]` when verified working, `[!]` when broken (add a note), `[skip]` if deferring.

---

## 0. Pre-flight

- [x] Backend on `slice-06-focus-lock`
- [x] Mac on `slice-13-cleanup`
- [x] iOS on `slice-12-puck-alarm-only`

---

## 1. Mac smoke

### 1.1 Build
- [x] `xcodebuild` returns BUILD SUCCEEDED

### 1.2 Launch
- [x] App launches without crash (Xcode Run)
- [x] Dashboard window opens
- [ ] **Sidebar shows 5 items: Today / Focus Modes / Sensitive Content / Accountability / Settings**
  - 1st attempt: saw OLD 8-item sidebar (stale Xcode cache).
  - 2nd attempt: launched fresh build, 4-item sidebar (Today/Week/Focus Modes/Settings).
  - **Iteration:** user pushed back — Sensitive Content + Accountability are FEATURES not Settings; restored to top-level. Today/Week becomes view-toggle on page-today (deferred build). Weekly Planning killed entirely.
  - 3rd attempt: rebuilt 5-item sidebar, app re-launched, **spec §8.2 updated**.
  - **Awaiting:** user confirms 5 items visible.

#### Slice 10 follow-up (deferred)
- [ ] Today/Week view-toggle UI at top of `page-today` (segmented control; default Today)
- [ ] Settings sub-sections render: Distractions list-management, Always-Blocked list management
- [ ] Folded `page-distractions` / `page-weekly` divs removed once their UI lives in new homes (slice 13)
- [ ] Click each sidebar item — page renders without error
- [ ] Pill widget visible + behaves as before
- [ ] Logs show `✅ EntitlementClient started`
- [ ] No red lapsed banner across top

### 1.3 Quick behavior
- [ ] Settings → toggle something → it persists
- [ ] Manual focus session start → pill updates correctly
- [ ] Wait 60s → no crash from EntitlementClient polling

---

## 2. iOS smoke

### 2.1 Build
- [ ] `xcodebuild -destination "generic/platform=iOS Simulator"` returns BUILD SUCCEEDED

### 2.2 Run on simulator
- [ ] App launches without crash
- [ ] Sign-in or LandingView shown
- [ ] Sign-in screen has "Subscribe at intentional.app →" link below magic-link button
- [ ] Tap bar shows **3 tabs: Today / Week / Settings**
- [ ] Tap Today → HomeView renders
- [ ] Tap Week → ScheduleTabView renders
- [ ] Tap Settings → SettingsView renders
- [ ] EntitlementGate decision: shows ContentView (paid) or LandingView (unpaid) without infinite loading

### 2.3 Sign-in flow (optional)
- [ ] Type email → Send magic link → arrives in inbox
- [ ] Tap link → app advances to ContentView or LandingView per backend decision

---

## 3. Backend (deferred until live deploy)

### 3.1 Static check
- [ ] Migrations 021–025 present in `intentional-backend/migrations/`

### 3.2 Live deploy + Stripe configure (T7) — manual only
- [ ] Apply migrations 021, 022, 023, 024, 025 via Supabase SQL editor (in order)
- [ ] Set Railway env: `STRIPE_SECRET_KEY`
- [ ] Set Railway env: `STRIPE_WEBHOOK_SECRET`
- [ ] Set Railway env: `STRIPE_PRICE_ID_MONTHLY`
- [ ] Set Railway env: `STRIPE_PRICE_ID_ANNUAL`
- [ ] Merge `slice-06-focus-lock` to main, push, Railway auto-deploys
- [ ] Stripe Dashboard webhook endpoint configured for the 6 events
- [ ] `stripe listen --forward-to https://api.intentional.social/stripe/webhook` succeeds
- [ ] `stripe trigger customer.subscription.created` returns 200, Railway logs show subscription updated

---

## 4. End-to-end (after T7 deploy)

- [ ] Visit puck-site → "Try free for 7 days" on Annual
- [ ] Stripe test card `4242 4242 4242 4242` → checkout completes
- [ ] Railway logs: `subscription updated: account ... → trialing (annual)`
- [ ] Mac dashboard within 60s: NO lapsed banner (you're trialing)
- [ ] iOS app: ContentView shown (subscribed)
- [ ] Cancel subscription in Stripe Dashboard
- [ ] SQL update: `users.current_period_ends_at` = 25h ago
- [ ] Mac dashboard within 60s: lapsed banner appears
- [ ] iOS within ~5s of foreground: kicks back to LandingView
- [ ] Reactivate in Stripe → both clear within 60s

---

## 5. Regression hunt

For each, expected behavior in parens. Mark `[!]` if broken.

- [ ] Sidebar nav: each Mac sidebar item navigates correctly (no dead clicks)
- [ ] Lapsed banner: hidden by default (only shown when entitlement says hard-lapsed)
- [ ] iOS tabs: 3 items present, each tab renders without missing UI
- [ ] Strict Focus Mode end-early: clicking End Block on a Strict block should NOT end the block (logged "End Block ignored — Focus Mode is Strict")
- [ ] Budget dual-write: distraction app for 1 min during a Focus block — local pool decrements; Console.app may show postBudgetConsume errors (expected without backend deployed) but no crashes
- [ ] Settings toggles: change Sensitive Content / other Settings toggle → persists
- [ ] Focus Modes page (was Intentions tab): renders the existing intentions list without errors
- [ ] iOS sign-in: magic-link email arrives, tap-to-verify works

---

## 6. Reporting back

When something breaks:
1. Note which test number above
2. Describe what you saw vs expected
3. Console.app source=`Intentional` for Mac issues
4. Xcode console for iOS issues

Drop the report into chat — I'll fix on the same branch and tell you to re-test.

---

## 7. Outstanding caveats (NOT bugs — known deferred work)

These are "minimum-viable" infrastructure items shipped this round; they will look incomplete in real use:

- Slice 6 (Focus Lock): backend tables + endpoints exist; **Mac UI toggle on Today page not yet built**. Today page is unchanged.
- Slice 9 (Block lifecycle): **5-min extend prompt + Standard 10s confirmation dialog** are TODOs in code, not built. Strict gate works.
- Slice 10 (Mac sidebar): old page divs (`page-distractions`, `page-sensitive`, `page-weekly`, `page-lock`) are still in DOM, just hidden from sidebar. Settings sub-sections that absorb them are NOT yet rendered — clicking a folded path may show empty.
- Slice 11 (iOS tabs): old views (IntentionsTabView, WakeView, PartnerView) still in source but unreferenced. Settings sub-sections to absorb them not yet built.
- Slice 12 (Puck alarm-only): `PUCK_ALARM_ONLY_MODE = true` flag is set, but the actual lock-screen-until-puck-tap alarm UI is not built — current alarm flow is unchanged.
- Slice 13: deletion checklist only, no actual deletions performed.
- T7 Stripe deploy: still your manual task.

If a test fails because of one of the above caveats, mark `[skip]` not `[!]`.

---

**TL;DR:** Live checklist. `[x]` = passed, `[!]` = broken (note details), `[skip]` = deferred. Update as you go. The Mac sidebar bug you just hit is a stale Xcode cache — clean build fixes it.
